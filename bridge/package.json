{
  "name": "velquor-bridge",
  "version": "1.0.0",
  "description": "VELQUOR MT5 bridge server — receives EA data and writes to Supabase",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "helmet": "^7.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
